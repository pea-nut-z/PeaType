import React, { useEffect, useState } from "react";

export default function TypeBox({ lang, time }) {
  const [timer, setTimer] = useState(time);
  const [start, setStart] = useState(false);

  useEffect(() => {
    return () => {
      second;
    };
  }, [third]);

  return (
    <section>
      <div></div>
      TypeBox
    </section>
  );
}
